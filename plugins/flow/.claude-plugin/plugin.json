{
  "name": "flow",
  "version": "0.1.0",
  "description": "A project-shaped team of long-lived AI agents that drives a continuous-flow backlog through dev, review and merge.",
  "mcpServers": {
    "flow": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/dist/index.js"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
