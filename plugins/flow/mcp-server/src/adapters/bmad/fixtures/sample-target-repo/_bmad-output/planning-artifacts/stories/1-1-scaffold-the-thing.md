# Story 1.1: Scaffold the thing

Status: backlog

## Story

As a **fixture story**,
I want **a plain `**AC1:**` heading**,
so that **the kind-tagger sees the default `unit` branch.**

## Acceptance Criteria

**AC1:**
**Given** a default-tag AC,
**When** the parser runs,
**Then** the kind is `unit`.

## Dev Notes

Minimal fixture for the unit-kind branch.
