# Story 1.2: Add a feature

Status: ready-for-dev

## Story

As a **fixture story**,
I want **a `user-surface` AC**,
so that **the kind-tagger normalises it to `integration`.**

## Acceptance Criteria

**AC1 (user-surface):**
**Given** a user-surface AC,
**When** the parser runs,
**Then** the kind is `integration`.
<!-- HTML comments inside an AC body must be stripped from the captured text. -->

**AC2:**
**Given** a plain AC,
**When** the parser runs,
**Then** the kind is `unit`.
