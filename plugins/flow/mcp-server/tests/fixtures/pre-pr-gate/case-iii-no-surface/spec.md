# Story X.Y: Fixture — no user-surface AC

## Acceptance Criteria

**AC1:**
**Given** an internal helper function,
**When** it is called,
**Then** it returns the expected value.

**AC2 (integration):**
**Given** another internal helper,
**When** called,
**Then** something else.
