# Fixture for tests/scan-sources.test.ts — do not hand-edit; regenerate from the BMad spike if the format changes.
adapter: bmad
adapter_config:
  stories_root: _bmad-output/planning-artifacts/stories
plugin: {}
