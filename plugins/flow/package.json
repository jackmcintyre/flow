{
  "name": "flow",
  "version": "0.1.0",
  "private": true,
  "description": "flow - a Claude Code plugin that runs a continuous-flow backlog through an AI engineering team.",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "knip": "knip --no-progress",
    "dev:install": "./scripts/dev-install.sh"
  },
  "devDependencies": {
    "knip": "^6.14.2"
  }
}
